database:
  override:
    - bin/setup
test:
  override:
    - bin/rake
deployment:
  staging:
    branch: master
    commands:
      - git remote add staging git@heroku.com:<%= app_name.dasherize %>-staging.git
      - bin/deploy staging
machine:
  node:
    version: 8.9.1
